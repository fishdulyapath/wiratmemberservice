# Database
DB_HOST=localhost
DB_PORT=5432
DB_NAME=your_database
DB_USER=your_user
DB_PASSWORD=your_password

# JWT
JWT_SECRET=your-secret-key-change-this
JWT_EXPIRES_IN=24h

# Server
PORT=3000

# Point Calc Timer (cron expression - every 30 minutes)
POINT_CALC_CRON=*/30 * * * *
